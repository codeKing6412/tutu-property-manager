# 验收记录

验证日期：2026-09-06。环境：Windows、本机 Python、Microsoft Edge。正式数据库与虚构演示数据库隔离。

## 收费与数据自动测试

`python -B -m unittest discover -s tests -v`：15项通过。

| 验证内容 | 结果 |
| --- | --- |
| 按月出账、重启补齐及重复生成去重 | 通过 |
| 房屋覆盖费率、0元费率及十进制四舍五入 | 通过 |
| 已售车位管理费、已租车位年租金、固定及面积追加费 | 通过 |
| 部分缴费、未来账单预缴、拒绝超收和撤销后恢复待缴 | 通过 |
| 同笔含无效分配时整笔不入账、拒绝未来收款日期 | 通过 |
| 房屋 / 业主 / 车辆 / 车位关联校验与重复车牌拦截 | 通过 |
| 临停数量计费和历史欠费补录 | 通过 |
| Excel往返读写、导入预览不写入及错误批次整体回滚 | 通过 |
| 已生成账单保留原价、新月份采用更新费率 | 通过 |
| 重复付款请求防重 | 通过 |
| Excel原生日期识别、非法起算年份拦截 | 通过 |
| 整栋、整单元、指定车辆及指定车位收费范围 | 通过 |
| 自定义每N个月收费周期 | 通过 |
| 停车管理费与公共收益合并缴费、分项核算 | 通过 |
| 12个月服务期、次年续费日及提前提醒 | 通过 |
| 旧版停车总价和历史账单安全拆分、付款总额保持 | 通过 |
| 账单金额调整入口、调整原因留痕及已收金额下限 | 通过 |

## 实际浏览器验收

`tests/browser.cjs` 使用独立演示服务（8766端口）自动操作，覆盖：

- 工作台及全部7个页面加载。
- 车牌模糊搜索反查房屋与业主。
- 侧边详情卡房屋、业主、车辆三页切换。
- 通过收款表单登记1.25元部分付款，生成打印收据并执行撤销。
- 生成两年预缴账单；同范围再次生成新增数量为0。
- 通过表单新增房屋，导航树自动更新。
- 导出真实Excel工作簿。
- 完整备份、修改数据、恢复备份，核对房屋、账单、收款数量及测试待办恢复。
- 0费率在数据接口中的金额为0。
- 1440、900、760像素宽度无页面横向溢出。
- 无浏览器脚本异常。

第二轮浏览器验收还覆盖：自定义车位状态、480元管理费与1320元公共收益一次缴纳、整栋收费范围、指定车辆收费规则、可维护住户身份与收款方式。

第三轮浏览器验收覆盖：“图图物业管家”名称显示；本月、本季度、本年度、上年度、近12个月及自定义应收核算区间；全部小区、整栋、整单元、单套房及费项范围；应收、已收、未收、到期欠费和收缴率金额核对；底部未收账单随同一组条件更新；“新增车位状态”及状态默认年度收费移至车位管理，并置于状态统计与具体车位列表上方。正式账本验收全程只读，前后档案、账单和收款数量一致。

正式数据库升级后执行了只读验收：421套房屋、425位住户、166辆车、154个车位和2笔付款均保留；183张旧版停车合计账单拆成管理费与公共收益，原账单总额及付款分配总额保持不变。正式页面无浏览器异常。

升级完成时的年度账单快照：2025年已收0.00元、未收522,520.44元；2026年已收350.24元、未收512,622.46元。该口径按账单归属年度计算，并包含系统已经出具的下月账单；之后会随收款和规则调整实时变化。

人工查看了工作台和详情卡截图，并修复菜单文字换行问题。

## 使用边界

- 正式库已包含用户导入的小区档案；自动测试只验证计算与关联逻辑，实际历史账目仍需用户按原始票据抽查确认。
- 尚未在用户的Excel桌面应用中人工打开导出表；已通过XLSX结构、读写往返及下载测试。
- 费用按整月计算，停车年度费按12个月一期并分项计入应收。
- 已生成的历史和远期账单锁价，费率变更不自动重写。
- 退款采用整笔原单冲销，非独立负向现金流；临停按手动输入数量计算。
- 未进行大型小区的容量压力测试。

正式库已含导入数据。升级前已生成JSON与SQLite双重快照；建议先抽查一套1800元车位和一套特殊金额车位，确认拆分结果符合实际口径。

## 正式启动验证

正式地址：http://127.0.0.1:8877。已验证停止、重新启动、API读取及正式页面只读筛选；正式库保留421套房屋、425位住户、166辆车和154个车位。8877避开了本机已有服务占用的8765端口。

